#include<stdio.h>

  main()
  {
  	 int year;
  	 printf("Enter year =");
  	 scanf("%d",&year);
  	 if(year%4==0)
  	 {
  	 	 printf("you entered year is a leap year");
  	 	 }
  	 else
	 {
	 	 printf("you entered year is not a leap year");
			}  	 
	 		
  }
